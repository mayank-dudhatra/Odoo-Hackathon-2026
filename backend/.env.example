# PostgreSQL connection string
# Example: postgresql://postgres:postgres@localhost:5432/peoplepay360
DATABASE_URL=postgresql://neondb_owner:npg_SeaG92yxvFLc@ep-wispy-dust-aek1bixh-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Optional: force SSL (useful for managed Postgres)
PG_SSLMODE=require
PORT=5000
NODE_ENV=development

# JWT authentication
ACCESS_TOKEN_SECRET=replace_with_long_random_access_secret
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_SECRET=replace_with_long_random_refresh_secret
REFRESH_TOKEN_TTL_DAYS=7

# Invitation and password security
INVITATION_TTL_HOURS=48
BCRYPT_ROUNDS=12

# Frontend base URL for activation link generation (dev helper)
FRONTEND_BASE_URL=http://localhost:5173

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

EMAIL_USER=
EMAIL_PASS=

# Password Reset Configuration
PASSWORD_RESET_URL=http://localhost:5173/reset-password
PASSWORD_RESET_TTL_MINUTES=30

